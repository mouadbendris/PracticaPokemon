<UserControl x:Class="PracticaPokemon.Controls.Pokemones"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PracticaPokemon.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" FontStyle="Normal" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Arial Black" Text="{Binding RelativeSource={RelativeSource AncestorType=local:Pokemones},Path=ThePokemon.PokId,FallbackValue=2}"></TextBlock>
        <TextBlock Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="15" FontFamily="Arial Black" Text="{Binding RelativeSource={RelativeSource AncestorType=local:Pokemones},Path=ThePokemon.PokName}"></TextBlock>

        <Image Grid.Row="2" Width="300" Source="{Binding RelativeSource={RelativeSource AncestorType=local:Pokemones},Path=ThePokemon.PokImage, IsAsync=True}"></Image>

        <ItemsControl HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="3" ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=local:Pokemones}, Path=ThePokemon.PokemonTypes}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"></StackPanel>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <local:Tipus TheType="{Binding Type}"></local:Tipus>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>