<UserControl x:Class="PracticaPokemon.Controls.PokemonCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PracticaPokemon.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border Background="White" CornerRadius="10" Margin="5" Padding="10">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!--Binding per la imatge, utilitzant relativesource per la propdp-->
            <Image Grid.Column="0" 
                   Source="{Binding RelativeSource={RelativeSource AncestorType=local:PokemonCard},Path=ThePokemon.PokImage}" 
                   Width="80" Height="80" 
                   VerticalAlignment="Center" HorizontalAlignment="Center"/>

            <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">

                

                <ItemsControl ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=local:PokemonCard},Path=ThePokemon.PokemonTypes.Count}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"></StackPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{Binding RelativeSource={RelativeSource AncestorType=local:PokemonCard},Path=ThePokemon.PokemonTypes}" 
                                    CornerRadius="3" 
                                    Padding="10,4" 
                                    Margin="0,0,5,0">
                                <TextBlock Text="{Binding Type.TypeName}" 
                                           Foreground="White" 
                                           FontWeight="Bold" 
                                           FontSize="10" 
                                           />
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </StackPanel>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <ItemsControl Grid.Row="2" ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=local:PokemonCard}, Path=ThePokemon.PokemonTypes}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"></StackPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:Tipus TheType="{Binding Type}"></local:Tipus>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="No. " Foreground="Gray" FontWeight="Bold" FontSize="14"/>
                    <!--Binding per el id, utilitzant relativesource per la propdp-->
                    <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=local:PokemonCard},Path=ThePokemon.PokId}" Foreground="Gray" FontWeight="Bold" FontSize="14" Margin="0,0,10,0"/>

                    <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=local:PokemonCard}, Path=ThePokemon.PokName}" 
               FontWeight="Bold" 
               FontSize="16" 
               Foreground="DarkGray"
               TextDecorations="Underline"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
